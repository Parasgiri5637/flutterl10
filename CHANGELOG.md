## 0.1.0

- Initial release.
- Adds `scan`, `apply`, `check`, and `gen` commands.
- Detects common static Flutter UI text patterns in `lib/**/*.dart`.
- Updates `app_en.arb` and replaces literals with `AppLocalizations.of(context)!` keys.
